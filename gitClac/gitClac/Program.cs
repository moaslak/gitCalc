using System;

namespace gitClac; // Note: actual namespace depends on the project name.

internal class Program
{
    static void Main(string[] args)
    {
        while (true)
        {
            menuClass menu = new menuClass();
        }
        
    }
}
